# Arcade Green Room

Green coffee operations app for Arcade Coffee Roasters (Riverside, CA):
weekly roast calendar, green coffee position, orders, importers, and
per-coffee inventory runway.

Single static page (`index.html`), no build step, no dependencies to
maintain. Data lives in Firestore behind Google sign-in with a team
allowlist; this repo contains only the app code.
